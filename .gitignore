__pycache__/
checkpoint/
*.log

data/
*.pth

# LLM-generated descriptions: keep tarballs in git, raw JSONs local only
cache/llm_descriptions/*/
!cache/llm_descriptions/*.tar.gz

# Cached feature banks / teacher logits / prototype seeds (large binaries, regenerable)
cache/*.pt
tools/harness/cache/
